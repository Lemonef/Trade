# graphify internal temp files (keep graph.html / graph.json / GRAPH_REPORT.md / cost.json)
graphify-out/.graphify_*

# backtest price data (re-fetchable via backtest/fetch_universe.py)
backtest/data/
__pycache__/
*.pyc

# live bot runtime state/logs (code is tracked, runtime data is not)
live_bot/state*.json
live_bot/*.csv
live_bot/*.log
web/data.json

# OS / editor cruft
Thumbs.db
.DS_Store
*.tmp
